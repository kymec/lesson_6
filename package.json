{
  "name": "lesson6",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel ./index.html",
    "build": "rm -rf dist && parcel build ./index.html --public-url /kymec",
    "deploy": "gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "camelcase": "^5.3.1"
  },
  "devDependencies": {
    "gh-pages": "^2.0.1",
    "parcel": "^1.12.3",
    "sass": "^1.20.3"
  }
}
